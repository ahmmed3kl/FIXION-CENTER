import { Navigate, Outlet, Route, Routes, useLocation, useNavigate } from "react-router-dom";
import { clearSession, getSession } from "./auth/authStore";
import { LoginPage } from "./pages/LoginPage"; import { DashboardPage } from "./pages/DashboardPage"; import { CentersPage, CenterDetailsPage } from "./pages/CentersPage"; import { UsersPage } from "./pages/UsersPage"; import { ServicesPage } from "./pages/ServicesPage"; import { CardRangesPage } from "./pages/CardRangesPage"; import { DevicesPage } from "./pages/DevicesPage"; import { SyncPage } from "./pages/SyncPage"; import { AuditLogsPage } from "./pages/AuditLogsPage"; import { HealthPage } from "./pages/HealthPage";
function ProtectedRoute(){const location=useLocation();return getSession()?<Outlet/>:<Navigate to="/login" replace state={{from:location.pathname}}/>;}
function AppShell(){const navigate=useNavigate();const location=useLocation();const session=getSession();const links=[['Dashboard','/dashboard'],['Centers','/centers'],['Users','/users'],['Services','/services'],['Card ranges','/card-ranges'],['Devices','/devices'],['Sync','/sync'],['Audit logs','/audit-logs'],['Health','/health']];return <div className="app-shell"><aside className="sidebar"><div className="brand"><img className="brand-logo" src="/branding/fixion-icon.png" alt="FIXION"/><span>FIXION</span></div><p className="eyebrow">Platform administration</p><nav>{links.map(([label,path])=><button className={location.pathname===path?"nav-item active":"nav-item"} key={path} onClick={()=>navigate(path)}><span>{label}</span><span className="nav-arrow">‹</span></button>)}</nav><div className="sidebar-footer"><div className="sidebar-status"><i/>Backend connected</div><button className="logout-button" onClick={()=>{clearSession();navigate('/login',{replace:true});}}>Sign out</button></div></aside><div className="main-column"><header className="topbar"><div className="topbar-title"><span className="eyebrow">Platform dashboard</span><h1>Welcome back</h1></div><div className="account"><span className="account-avatar">{(session?.admin.name??session?.admin.email??'A').slice(0,1).toUpperCase()}</span><span>{session?.admin.name??session?.admin.email}</span><span className="role-badge">Platform Admin</span></div></header><main className="page-content"><Outlet/></main></div></div>}
export default function App(){return <Routes><Route path="/login" element={<LoginPage/>}/><Route element={<ProtectedRoute/>}><Route element={<AppShell/>}><Route path="/dashboard" element={<DashboardPage/>}/><Route path="/centers" element={<CentersPage/>}/><Route path="/centers/:centerId" element={<CenterDetailsPage/>}/><Route path="/users" element={<UsersPage/>}/><Route path="/services" element={<ServicesPage/>}/><Route path="/card-ranges" element={<CardRangesPage/>}/><Route path="/devices" element={<DevicesPage/>}/><Route path="/sync" element={<SyncPage/>}/><Route path="/audit-logs" element={<AuditLogsPage/>}/><Route path="/health" element={<HealthPage/>}/><Route path="*" element={<Navigate to="/dashboard" replace/>}/></Route></Route><Route path="/" element={<Navigate to="/dashboard" replace/>}/></Routes>;}
