import { FormEvent, useState } from "react";import { useLocation,useNavigate } from "react-router-dom";import { ApiError,platformApi } from "../api/client";import { setSession } from "../auth/authStore";
export function LoginPage(){const navigate=useNavigate();const location=useLocation();const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(event:FormEvent){event.preventDefault();setError("");setLoading(true);try{const result=await platformApi.login(email.trim(),password);setSession(result.session);navigate((location.state as {from?:string}|null)?.from??"/dashboard",{replace:true});}catch(err){setError(err instanceof ApiError?err.message:"تعذر تسجيل الدخول.");}finally{setLoading(false);}}return <div className="auth-page"><div className="auth-card"><div className="auth-hero"><img src="/branding/fixion-icon.png" alt="FIXION"/><div><strong>FIXION</strong><span>Platform Console</span></div></div><div className="auth-copy"><p className="eyebrow">SECURE PLATFORM ACCESS</p><h1>تسجيل دخول المسؤول</h1><p className="muted">تحكم في المراكز والمستخدمين والمزامنة من لوحة واحدة.</p></div><form onSubmit={submit}><label>البريد الإلكتروني<input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="username" placeholder="admin@fixion.com" required/></label><label>كلمة المرور<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" placeholder="••••••••••••" required/></label>{error&&<div className="alert error" role="alert">{error}</div>}<button className="primary-button login-submit" disabled={loading}>{loading?"جارٍ التحقق...":"دخول إلى لوحة التحكم"}<span>←</span></button></form><div className="auth-footer"><span className="secure-dot"/> اتصال آمن عبر Backend المركزي</div></div></div>}
